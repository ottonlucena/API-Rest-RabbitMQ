server:
  port: 9090

dataservice:
  url: http://service-data:8090

spring:
  application:
    name: mscv-orderService
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-order:3306/orderDB
    username: root
    password: entrevista
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: user
    password: password

